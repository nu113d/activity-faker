# activity-faker
A tiny Python script that moves the cursor by a few pixels every minute just to look busy on Teams and other simillar programs.
Also, it prevents the screen from hibernating.

Python modules required:
```
pyautogui
wakepy
```

Run with
```
python mouse.py
```
Make sure the window of the program you want (e.g. Teams, Discord) is focused
